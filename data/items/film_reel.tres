[gd_resource type="Resource" script_class="ItemData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/systems/item_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "film_reel"
display_name = "Film Reel"
description = "A second part — alongside the projector repair — needed to restore the Grand Marquee's projector."
icon_color = Color(0.35, 0.32, 0.3, 1)
owner_character = ""
is_junk = false
